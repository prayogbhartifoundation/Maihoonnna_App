import {
  SubscriptionPackage as PrismaSubscriptionPackage,
  Subscription as PrismaSubscription,
} from '@prisma/client';

export type SubscriptionPackage = PrismaSubscriptionPackage;
export type Subscription = PrismaSubscription;